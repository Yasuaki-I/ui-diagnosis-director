# 🔄 新環境引継書｜統括担当（Claude-Chat）→ 次期統括担当｜2026-09-22（火・祝）発行

> 本書は統括担当（ディレクター側）の次期セッション起動時の起点文書である。
> AIスライド（実装領域）側の引継はAIスライド自身の `NEXT_ENV_HANDOVER_20260922_aislide.md` を別途参照する。
> 配置：入江さん経由でAIスライドが配置する（第8条 単一書き込み者原則）。
> 前版：`NEXT_ENV_HANDOVER_20260921_director.md`（8,997B／`272d44920ea8`｜無変更で保存）
> 命名・版管理：第18条 準拠｜第18条 細則2 準拠（日付繰り上げ・前版無変更保存）

## ■ 1. 本書の位置づけ
- 前版：`NEXT_ENV_HANDOVER_20260921_director.md`（2026-09-21 発行）
- 今回：2026-09-22 セッションが25往復に到達したため、次期統括担当セッションの起点として更新発行
- 運用ルール：各往復冒頭に「X往復目／25往復（日付 曜日）」を明記し、ユーザー（入江さん）の判断を待つ

## ■ 2. 確認済み値（2026-09-22 終業時点）
| 項目 | 値 |
|---|---|
| 採用版 builder | **v17.2.13（md5 `c11ab9642b8e`／278,135B）｜2026-09-22 配布依頼中（未完了）** |
| 採用版 PLAIN | `01_Instructions_v3.5_PLAIN_20260922.md`（67,538B／md5 `2f9a1a364fca`）｜配布対象外（社内原本） |
| 採用版 lite | v3.5.2（`61b5f4e7b928`／16,223B）｜2026-09-18 配布済・無変更 |
| 運用ルール条文 | **`OPERATIONAL_RULES_MASTER_20260922.md`（112,604B／md5 `252a44586e2d`）｜全19条＋第16条 細則15 を新設** |
| 監視台帳 | `bottom_margin_watchlist_20260921.md`（17,883B／md5 `db7452ea5392`）｜登録1件（funnel +3.0px） |
| 事例台帳 | `ops_rule_article10_case_registry.md`（168,715B／md5 `7159534def90`）※直近無変更 |
| 常設検証スクリプト | `handover/scripts/` に**5本**常設（verify_md5／verify_frozen／write5／find_prefix／**verify_render**）｜`verify_labels.py` は廃止スタブ |
| 成果物マニフェスト | `handover/scripts/manifest_20260922.tsv`（27点／NG 0件） |
| 必達ルール9 継続観測 | **14日目・累計133点・消失0件・NG0件** |
| 判定待ち | 0件 |
| 新規課題 | 3件（N-1：判定書ファイル未配置／N-2：正規表現棚卸し・低／**N-3：第18条に同日2回目改稿の規定なし・低**） |

## ■ 3. 主要な意思決定履歴（要約）

### 3-1. 9/21 までの決定（前版から継承）
1. **文字幅推定器の全面刷新**：安全係数1.35頼みから、フォント本体実測（Meiryo W53 Bold）に基づく`_V17_W14`全面置換へ移行。安全係数も1.35→1.15へ適正化。推定誤差 4.78%→0.30%。
2. **文字揃え分岐（J-7-6）の適用範囲確定**：見出しが中央揃えのパターン（pyramid・funnel）にのみ適用し、他11パターンは変更なしで確定。
3. **回帰検証手順の条文化（PLAIN J-7-6-2 A）**：推定器・安全係数の変更時は「折返し判定」と「枠幅算出」を分けて回帰検証する。
4. **配布状態の記録様式条文化（第16条 細則14）**：配布完了連絡を受けた往復で「配布済」へ更新、配布済の再依頼禁止。
5. **条文改稿手順の条文化（第18条 細則2）**：新版発行・既存版の直接上書き禁止・前版の無変更保存。
6. **順5（型02・03新設）の完結**：既存C-3への最小限の追記で完結。
7. **単一書き込み者原則の運用確立**：統括担当はAIドライブへ直接書き込まず、入江さん経由でAIスライドが配置する。

### 3-2. 🔴 9/22 の決定（本セッション｜3便・計6判定）

#### 判定①（第2便・第3便）｜描画不具合 3件の是正
| # | 事象 | 是正 | 版 |
|:---:|---|---|---|
| 1 | ラベル「次アクション」（112.0px）が枠 90px を超過し折返し | 「次の一手」へ変更（74.7px） | v17.2.12 |
| 2 | 太字スコア値 `4.0/5`（55.5px）が枠 50px を超過し折返し | 枠 50→62px | v17.2.13 |
| 3 | 項目名 `ファーストビュー訴求`（186.7px）が枠 180px を超過し折返し | **切詰を字数基準→幅基準へ（`_truncate_px` 新設）** | v17.2.13 |

**#3 は第2便判定（字数 10→9字）では是正されなかった。** AIスライドの報告誤り（実測値 168.0px は「表示9字＝実装引数8」の値であったのを引数「9」と取り違え）が原因。第3便で幅基準（案B）へ切り替え、副作用0件で確定した。

#### 判定②（第2便）｜検証方式を静的解析から生成物実測へ転換
- **`verify_render.py` を常設化**（生成済 `.pptx` の全テキスト段落を実測）。`verify_labels.py` は統合廃止。
- 理由：静的解析は f-string・変数・関数戻り値で組み立てた文字列を原理的に検出できず、9/22 に実機目視で発見された2件はいずれもこの死角にあった（`verify_labels.py` は「NG 0件」を返していた）。
- **PLAIN J-7-6-2 B を全面改稿**（B-1 死角の明示／B-2 LibreOffice 不可／B-3 確認手段の明示）。

#### 🚨 LibreOffice は PowerPoint 実機の代替にならない（9/22 実測確定）
| 検証項目 | フォント実測 | LibreOffice | PowerPoint 実機 |
|---|:---:|:---:|:---:|
| 太字スコア値の折返し | **一致 4/4** | 🚨 **不一致 3/4** | 基準 |

LibreOffice は太字の描画幅を PowerPoint と異なる値で解決し、本来 NG の枠超過を「収まっている」と表示する。**信頼できる一次情報はフォント実測（`verify_render.py`）と PowerPoint 実機の画素確認の2つのみ。**

#### 🔴 確認手段の明示義務（第16条 細則15 新設）
「確認した」という報告には、下表のいずれで確認したかを必ず併記する。手段を明示しない「実機確認完了」「目視確認済」は禁止。

| 手段 | 呼称 | 実機の代替 |
|---|---|:---:|
| PowerPoint で画素を見る | **実機目視** | 基準そのもの |
| LibreOffice で画素を見る | LibreOffice 画素確認 | ❌ 不可 |
| `verify_render.py` で測る | フォント実測 | 折返し判定は可・最終確認は不可 |
| PPTX の座標値を読む | 座標確認 | ❌ 不可 |

制定理由：**確認手段の取り違えが3日連続で発生した**（9/21「実機目視完了」＝実際は座標確認／9/22 第1便「9字なら168.0px」＝実際は表示9字の値／9/22 第2便「画素で収容確認済」＝実際は LibreOffice 画素）。いずれも「測定した対象」と「報告に用いた語」が対応していない同一構造。

#### 🔴 数値報告時の入力併記（PLAIN J-7-6-4 新設）
実測値・算出値を報告するときは、値だけでなく「その値を生んだ入力（引数・条件・手段）」を必ず併記する。上記3件の再発防止を、口頭の申告ではなく条文として組み込んだもの。

## ■ 4. 次期セッションへの優先タスク
| # | 内容 | 優先度 |
|:---:|---|:---:|
| 1 | **builder v17.2.13（`c11ab9642b8e`）の配布完了確認**（連絡受領の往復で「配布済」へ更新／第16条 細則14） | **高** |
| 2 | **`verify_truncate_px_20260922.pptx`（`ad860215a512`）の PowerPoint 実機目視の結果確認** | **高** |
| 3 | **N-1（判定書ファイル未配置）の決着**（9/21・9/22 と2回持ち越し。無期限繰越をしない） | **高** |
| 4 | 必達ルール9 の継続観測（15日目以降） | 中（継続） |
| 5 | N-3（第18条への「同日2回目の改稿」規定の追加）の起案要否判断 | 低 |
| 6 | N-2（正規表現照合の棚卸し）の着手可否判断（対象は `find_prefix.py` のみに縮小） | 低 |
| 7 | v3.7候補（`v37_candidates_20260914.md`）の見直し要否確認 | 低 |

## ■ 5. 運用上の留意点（次期統括担当向け）
- 統括担当はAIドライブへの直接書込を行わない。判定・申し送りは全文をチャット本文で提示し、入江さんがAIスライドへ中継、AIスライドが配置する。
- 実装キューが空いていても、指示のない探索的着手は行わせない。
- 判定を仰ぐ諮問には必ず推し案の理由を明示させ、統括側も判断根拠を明示して判定すること。
- 各往復冒頭で「X往復目／25往復（日付 曜日）」を明記する運用を継続する。
- **統括側が画像でPPTXを確認した場合、それは「画像上の確認」であり、入江さんによる PowerPoint 実機目視の代替にはならない旨を明示すること**（9/22 統括自身が実践）。

## ■ 6. 次期統括担当への反省点

### 6-1. 往復番号カウントの管理徹底（9/21 から継続）
往復は「入江さんの発言1回＋AIスライドの応答1回」で1往復。ツール実行や連続発言は含まない。各往復冒頭で「X往復目／25往復（日付 曜日）」を明記させ、統括側も受領した番号の連続性を確認すること。

### 6-2. 判定確定時は「候補リストの区分更新」まで同時に指示する（9/21 から継続）
判定確定時に、当該項目を候補一覧のどの区分へ移すかまで併せて指示すること。

### 6-3. N-1（判定書ファイル未配置）は次期セッション開始時に決着をつける
9/21 §7-3 で「次期セッション開始時に決着」としたが、9/22 も持ち越しとなり**2回連続の繰り越し**となった。次期セッションでは冒頭で扱いを確定させること。

### 6-4. 🔴 判定の前提となる数値は「入力とセットで」報告させる（9/22 追加）
9/22 第2便では、AIスライドの報告した実測値「9字＝168.0px」を前提に判定を下したが、実際にはその値は「表示9字＝実装引数8」のものであり、承認どおり実装しても是正されなかった。**判定を出し直す事態となった。**

今後、実装値を伴う諮問を受ける際は、**「その数値を生んだ入力（引数・条件・手段）」の併記を求めること**（PLAIN J-7-6-4 として条文化済）。値だけの報告は、判定の前提そのものが崩れうる。

### 6-5. 🔴 「確認した」の手段を必ず確認する（9/22 追加）
9/21・9/22 と2日連続で、AIスライドの「実機目視完了」「画素で収容確認済」という報告が、実際には座標確認・LibreOffice 画素確認であった。いずれも入江さんの実機目視で不具合が発見されている。

**第16条 細則15 として条文化したが、統括側でも報告受領時に手段を確認すること。** 特に「実機」という語は、PowerPoint 以外を指していないかを疑う。

---
以上。次期統括担当セッションはこの文書を起点として開始してください。
